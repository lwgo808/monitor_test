pip freeze > requirements.txt

mediapipe==0.10.21
opencv-python==4.11.0.86
numpy==2.3.1
torch==2.7.1

#make sure you enable your virtual environment before running the above command to ensure that the requirements.txt file captures the correct dependencies for your project.

RUN THE FOLLOWING COMMANDS TO CREATE A VIRTUAL ENVIRONMENT AND INSTALL DEPENDENCIES:

python -m venv venv
source venv/bin/activate      # macOS/Linux
# or
venv\Scripts\activate         # Windows

# Then install the dependencies from the requirements.txt file:

pip install opencv-python mediapipe numpy matplotlib

pip install torch torchvision